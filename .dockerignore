# Keep the Docker build context small and free of host artifacts.
**/node_modules
**/dist
**/.turbo
**/.expo
**/.env
.git
.github
apps/mobile
# ...but keep the manifest so --frozen-lockfile can validate the full workspace
# (mobile's deps are excluded from install by --filter=@destow/api).
!apps/mobile/package.json
docs
