route:
  receiver: default
  group_by: ["alertname", "service"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: default

# Add a real receiver before production incident response depends on alerts.
# Examples:
# - Slack webhook
# - email / SMTP
# - PagerDuty / Opsgenie
