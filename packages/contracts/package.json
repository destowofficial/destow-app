{
  "name": "@destow/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Shared API contract for Destow: enums, response envelope, zod schemas. Single source of truth for backend + clients.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup",
    "lint": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.8.3"
  }
}
